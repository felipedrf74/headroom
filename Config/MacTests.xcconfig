// Unit tests hosted in Tokenroom for Mac. Signed like the host so library validation passes.
#include "Version.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(TOKENROOM_BUNDLE_PREFIX).mac.tests
MARKETING_VERSION = $(TOKENROOM_MAC_VERSION)
CURRENT_PROJECT_VERSION = $(TOKENROOM_MAC_BUILD)

#include "MacSigning.xcconfig"
